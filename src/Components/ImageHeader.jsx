import React from 'react'

const ImageHeader = ({
  activeImage,
}) => {
  return (
    <div
      style={{
        height: '40vh',
        backgroundColor: 'gray',
      }}
    >ImageHeader</div>
  )
}

export default ImageHeader